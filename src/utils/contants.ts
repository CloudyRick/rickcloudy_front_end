const API_URL = 'http://localhost:3000';

export const API_ROUTES = {
    SIGN_UP: `${API_URL}/api/v1/user/register`,
    SIGN_IN: `${API_URL}/api/v1/auth/`
}

export const APP_URL = {
    SIGN_UP: '/signup'
}